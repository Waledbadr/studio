# الإخراج التلقائي عند التحويل - Auto-Checkout on Transfer

## الوصف

تم تطبيق ميزة الإخراج التلقائي للعمال المحولين من سكن لآخر. عند تسكين عامل محول في السكن الجديد، يتم إخراجه تلقائياً من السكن القديم في اليوم السابق ليوم التسكين الجديد.

## مثال

**السيناريو:**
- تم تحويل العامل من السكن A إلى السكن B بتاريخ 01/01/2025
- تم تسكين العامل في السكن B بتاريخ 05/01/2025

**النتيجة التلقائية:**
- يتم إخراج العامل من السكن A بتاريخ **04/01/2025** (اليوم السابق للتسكين الجديد)

## التفاصيل التقنية

### 1. الدالة `checkInWorkerAsync`

تم تعديل الدالة لإضافة منطق الإخراج التلقائي:

```typescript
// عند تسكين عامل بحالة 'Transferring':
if (worker.status === 'Transferring') {
  // 1. البحث عن التسكين النشط الحالي
  // 2. حساب تاريخ الإخراج = تاريخ التسكين الجديد - يوم واحد
  // 3. تحديث سجل التسكين القديم بتاريخ الإخراج
  // 4. إضافة ملاحظة: "تم الإخراج تلقائياً عند التسكين في السكن الجديد"
}
```

### 2. الدالة `bulkCheckIn`

تم إضافة نفس المنطق للتسكين الجماعي:

```typescript
// لكل عامل في القائمة:
for (const worker of workersToProcess) {
  if (worker.status === 'Transferring') {
    // إخراج تلقائي من السكن القديم
    // باستخدام batch.update للكفاءة
  }
  
  // تحديث حالة العامل إلى 'Active'
  batch.update(doc(db, 'workers', worker.id), {
    status: 'Active',
    transferDestination: null
  });
}
```

## الملفات المعدلة

- `src/context/accommodation-context.tsx`
  - `checkInWorkerAsync` (السطر ~1404)
  - `bulkCheckIn` (السطر ~2661)
- `src/app/accommodation/pending-transfers/page.tsx`
  - منطق الفلترة لعرض العامل في كلا المدينتين (المصدر والوجهة)

## السلوك

### قبل التعديل
- كان يجب إخراج العامل يدوياً من السكن القديم
- إمكانية حدوث تضارب في البيانات (العامل مسكّن في مكانين)

### بعد التعديل
- ✅ إخراج تلقائي من السكن القديم
- ✅ تاريخ الإخراج = تاريخ التسكين الجديد - 1 يوم
- ✅ تحديث حالة العامل من `Transferring` إلى `Active`
- ✅ إضافة ملاحظات توضيحية تلقائياً

## الحالات الخاصة

### في حالة الفشل
- إذا فشل الإخراج التلقائي، يتم:
  - تسجيل تحذير في الكونسول
  - الاستمرار في عملية التسكين الجديد
  - لا يتم إيقاف العملية

### في حالة عدم وجود تسكين سابق
- لا يتم تنفيذ أي إجراء إضافي
- يتم التسكين بشكل طبيعي

## الرسائل في الكونسول

```
🔄 [Auto-Checkout] Worker {workerId} is Transferring. Auto-checking out from residence {residenceId} on {date}
✅ [Auto-Checkout] Successfully checked out worker from previous residence
⚠️ [Auto-Checkout] Failed to auto-checkout transferring worker (non-critical): {error}
```

## الاختبار

### سيناريو الاختبار الموصى به:

1. إنشاء عامل جديد
2. تسكينه في السكن A
3. تحويله إلى السكن B (تحديث الحالة إلى `Transferring`)
4. تسكينه في السكن B بتاريخ محدد
5. التحقق من:
   - ✅ تم إخراجه من السكن A بتاريخ (تاريخ التسكين - 1)
   - ✅ تم تسكينه في السكن B بالتاريخ المحدد
   - ✅ حالة العامل تغيرت إلى `Active`
   - ✅ `transferDestination` تم حذفه

## التوافق

- متوافق مع النظام الحالي
- لا يؤثر على العمال غير المحولين
- يعمل في وضع الطوارئ والوضع العادي

## التاريخ

- **التطوير:** 28 ديسمبر 2025
- **النسخة:** 1.1
- **الحالة:** مفعّل ✅

## التحديثات

### النسخة 1.1 (28 ديسمبر 2025)
- ✅ تعديل صفحة التحويلات المعلقة لعرض العامل في كلا المدينتين
- ✅ إضافة عمود "Current Location" لإظهار المدينة المصدر
- ✅ تحسين منطق الفلترة لدعم الظهور في المصدر والوجهة

### النسخة 1.0 (28 ديسمبر 2025)
- ✅ تطبيق الإخراج التلقائي عند التحويل
- ✅ تحديث حالة العامل تلقائياً
